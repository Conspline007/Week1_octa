#include <stdio.h>
#include <math.h>

int main()
{
	float x;
	printf("Enter a number to find its sqaure root ");
	scanf("%f",&x);
	printf("The square root of given number is %f",sqrt(x));
	return 0;
}
